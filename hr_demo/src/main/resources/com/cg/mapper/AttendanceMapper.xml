<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cg.mapper.AttendanceMapper">

    <select id="queryByEmployeeId" resultType="Integer">
        select department_id from employee where employee_id = #{employeeId};
    </select>
    <insert id="add">
        insert into attendance(employee_id,department_id, work_date, status)
        values (#{employeeId},#{departmentId},#{workDate},#{status})
    </insert>

</mapper>