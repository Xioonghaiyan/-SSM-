<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cg.mapper.StatsMapper">
    <select id="queryLeaves" resultType="Integer">
        select leave_days from leave_request
        where employee_id = #{employeeId} and MONTH(leave_time) = #{mouth}
    </select>
    <select id="queryDaysWorked" resultType="Integer">
        select count(status) from attendance
        where employee_id = #{employeeId} and status = 1 and MONTH(work_date) = #{mouth}
    </select>
    <select id="queryDaysMissed" resultType="Integer">
        select count(status) from attendance
        where employee_id = #{employeeId} and status = 0 and MONTH(work_date) = #{mouth}
    </select>
    <select id="queryDepartmentId" resultType="Integer">
        select department_id from leave_request
        where employee_id = #{employeeId}
    </select>
    <select id="queryDays" resultType="Integer">
        select count(*) from attendance where employee_id = #{employeeId} and status = 1
    </select>
    <select id="queryEmployeeIds" resultType="Integer">
        select employee_id from employee where department_id = #{departmentId}
    </select>
    <select id="queryLeavaDays" resultType="Integer">
        select leave_days from leave_request where employee_id = #{employee}
    </select>
</mapper>